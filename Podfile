# Uncomment the next line to define a global platform for your project
platform :ios, '15.5'

target 'Trial First App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  pod 'Charts'
  pod 'TinyConstraints'

  # Pods for Trial First App

  target 'Trial First AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Trial First AppUITests' do
    # Pods for testing
  end

end
